name: pipeline
on: ['push', 'pull_request']

#job - trabalho
jobs:
  # nome do job
  test:
    # qual so
    runs-on: ubunto-latest

    staps:
      - name: Instalar o Python
        # action pronta
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'
